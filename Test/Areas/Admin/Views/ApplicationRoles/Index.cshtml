@model IEnumerable<Test.Models.ApplicationRole>

@{
    ViewData["Title"] = "Role";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>@ViewData["Title"]</h1>
</section>

<!-- Main content -->
<section class="content">
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body">
                    <table class="table table-bordered table-striped" id="myTable">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                @*<th>
                                        @Html.DisplayNameFor(model => model.NormalizedName)
                                    </th>*@
                                <th>
                                    @*@Html.DisplayNameFor(model => model.ConcurrencyStamp)*@Deskripsi
                                </th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td></td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    @*<td>
                                            @Html.DisplayFor(modelItem => item.NormalizedName)
                                        </td>*@
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ConcurrencyStamp)
                                    </td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@item.Id"><i class="fa fa-file-text-o"></i></a>&nbsp;&nbsp;&nbsp;
                                        <a asp-action="Edit" asp-route-id="@item.Id"><i class="fa fa-edit"></i></a>&nbsp;&nbsp;&nbsp;
                                        <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa fa-remove"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
